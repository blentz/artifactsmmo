-------------------------------- MODULE Smoke --------------------------------
EXTENDS Integers, TLC

VARIABLE i

Init == i = 0
Next == /\ i < 3
        /\ Assert(i >= 0, <<"Smoke FAIL at", i>>)
        /\ i' = i + 1
=============================================================================
